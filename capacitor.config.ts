import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mboa_Culture',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
